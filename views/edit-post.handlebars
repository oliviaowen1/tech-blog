<article style="background-color: #6699cc">
  <a href="/dashboard" class="is-pulled-right"> &larr; back to dashboard</a>
  <h2 class="title mt-5">edit post</h2><hr>
  <form class="edit-post-form">
    <div>
        <label for="post-title">post title:</label><br>
      <input class="input" name="post-name" type="text" value="{{post.name}}" />
    </div>
    <div>
        <label for="post-desc">post content:</label><br>
      <input class="textarea" name="post-desc" type="text" value="{{post.description}}" />
    </div>
    <div class="is-pulled-right">
       created by you on {{format_date post.created_at}}
      |
      <a href="/post/{{post.id}}">{{post.comments.length}} {{format_plural "comment" post.comments.length}}</a>
    </div>
    <button type="submit" class="button is-success">save post</button>
    <button type="button" class="button is-danger delete-post-btn">delete post</button>
  </form>
</article>

<script src="/js/editpost.js"></script>

