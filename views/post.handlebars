<div class="row">
  <div class="col-md-8 p-3 bg-warning">
    <h2>{{name}}</h2>

    <div class="row">
      <div class="col-md-8">
        <p class="text-secondary">{{description}}</p>
      </div>
    </div>

  </div>
</div>
<div class="row">
  <div class="col-md-8 bg-success">
    <p class="text-secondary pt-2 text-right"> Posted by {{user.name}} on {{format_date date_created}}</p>
  </div>
</div>